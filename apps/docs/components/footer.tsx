import { Footer } from "nextra-theme-docs";

export const footer = (
  <Footer className="py-2 flex items-center justify-center !bg-background text-sm border-t-[0.5px]">
    MIT {new Date().getFullYear()} © Nextra.
  </Footer>
);
