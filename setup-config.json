{
  "introMessage": "🚀 Welcome to Turbo-Convex Starter! Let's set up your services and environment variables.",
  "projects": [
    {
      "id": "convex",
      "exportCommand": "npx convex env set {{name}} {{value}}",
      "importCommand": "npx convex env get {{name}}",
      "ignoreLogs": ["✖ Environment variable"]
    },
    {
      "id": "web",
      "envFile": "apps/web/.env"
    },
    {
      "id": "app",
      "envFile": "apps/app/.env"
    }
  ],
  "steps": [
    {
      "title": "📱 App URL Configuration",
      "description": "Set up the base URL for your application",
      "variables": [
        {
          "name": "NEXT_PUBLIC_APP_URL",
          "projects": ["web"],
          "details": "The URL where your app will be accessible. For local development, use the default value.",
          "defaultValue": "http://localhost:3000",
          "prompt": "Enter your app URL"
        }
      ],
      "interactive": false
    },
    {
      "title": "🔌 Convex Setup",
      "description": "Configure your Convex backend connection",
      "instructions": [
        "1. Go to https://dashboard.convex.dev",
        "2. Create a new project or select an existing one",
        "3. Copy your Deployment URL from Settings > URL & Deploy Key"
      ],
      "variables": [
        {
          "name": "NEXT_PUBLIC_CONVEX_URL",
          "projects": ["web", "app"],
          "details": "The URL that connects your app to your Convex backend. Format: https://[deployment-id].convex.cloud",
          "template": "{{convexUrl}}",
          "defaultValue": "https://example-123.convex.cloud",
          "prompt": "Enter your Convex deployment URL"
        }
      ]
    },
    {
      "title": "💳 Polar Integration",
      "description": "Set up subscription and billing with Polar",
      "instructions": [
        "1. Create an account at https://polar.sh",
        "2. Click 'Sell with Polar' to set up your organization",
        "3. For testing, use https://sandbox.polar.sh/dashboard/create",
        "4. Gather your credentials from the dashboard"
      ],
      "variables": [
        {
          "name": "POLAR_ORGANIZATION_ID",
          "projects": ["convex", "web", "app"],
          "details": "Found in Settings > General > Organization",
          "prompt": "Enter your Polar Organization ID"
        },
        {
          "name": "POLAR_ACCESS_TOKEN",
          "projects": ["convex", "web", "app"],
          "details": "Generate this token in Settings > General > Developers\nRequired permissions:\n- checkouts (read/write)\n- products (read/write)\n- subscriptions (read/write)\n- customers (read/write)\n- checkout_links (read/write)\n- customer_portal (read/write)\n- customer_sessions (write)",
          "prompt": "Enter your Polar Access Token"
        },
        {
          "name": "POLAR_WEBHOOK_SECRET",
          "projects": ["convex", "web", "app"],
          "details": "Create in Settings > Webhooks\nPayload format: Raw\nRequired events:\n- product.created/updated\n- subscription.created/updated",
          "info": ["Webhook URL: {{convexSiteUrl}}/payments/webhook"],
          "prompt": "Enter your Polar Webhook Secret"
        }
      ],
      "additionalInstructions": [
        "Note: The sandbox environment is recommended for initial testing"
      ]
    },
    {
      "title": "📧 Resend Email Service",
      "description": "Configure email notifications and verification",
      "instructions": [
        "1. Sign up at https://resend.com",
        "2. Generate an API key from the dashboard",
        "3. Verify your domain for sender email"
      ],
      "variables": [
        {
          "name": "AUTH_RESEND_KEY",
          "projects": ["convex"],
          "details": "Your API key from Resend dashboard > API Keys (starts with 're_')",
          "prompt": "Enter your Resend API key"
        },
        {
          "name": "AUTH_RESEND_SENDER_EMAIL",
          "projects": ["convex"],
          "details": "The email address you'll use to send verification emails. Must be verified in your Resend account.",
          "required": false,
          "prompt": "Enter your sender email address (optional)"
        }
      ],
      "required": false,
      "requiredMessage": "Email functionality will be limited until Resend is configured."
    }
  ]
}
